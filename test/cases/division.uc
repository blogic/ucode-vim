// Every slash here is division. None of it may colour as a regexp literal.
let z = (a / b) + (c / d);
let q = a / b / c / d;
let v = arr[0] / 2;
let w = f(1) / g(2);
let n = (a + b) / (c - d) / e;
let p = obj.count / 2 / 3;
let s = "one" / 2;
x /= 2;
