// Names that a JavaScript-shaped syntax file gets wrong. None of these words
// is reserved in ucode, so each stays a perfectly ordinary identifier.
let from = 1;
let as = 2;
let of = 3;
let value = from + as + of;
print(value);
